<div id="loader" class="loader-screen">
  <video autoplay muted loop playsinline class="loader-video">
    <source src="Video by Luis Quintero from Pexels: https://www.pexels.com/video/noise-video-4990235/" type="video/mp4" />
  </video>
  <div class="loader-texts">
    <h2 class="loader-text">Creating...</h2>
    <h2 class="loader-text">Designing...</h2>
    <h2 class="loader-text">Crafting experience...</h2>
    <h2 class="loader-text">Design Dipesh experience...</h2>
  </div>
</div>

<style>
  .loader-screen {
    position: fixed;
    inset: 0;
    background: #000;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    color: white;
    font-family: sans-serif;
    overflow: hidden;
  }

  .loader-video {
    position: absolute;
    object-fit: cover;
    width: 100%;
    height: 100%;
    z-index: -1;
    opacity: 0.4;
  }

  .loader-texts {
    text-align: center;
    z-index: 2;
  }

  .loader-text {
    font-size: 2rem;
    opacity: 0;
    animation: fadeInText 1s forwards;
  }

  .loader-text:nth-child(1) { animation-delay: 0s; }
  .loader-text:nth-child(2) { animation-delay: 1.5s; }
  .loader-text:nth-child(3) { animation-delay: 3s; }

  @keyframes fadeInText {
    to {
      opacity: 1;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const loader = document.getElementById('loader');
    const main = document.getElementById('MainContent');

    setTimeout(() => {
      loader.style.display = 'none';
      main.classList.remove('hidden');
    }, 5000);
  });
</script>
